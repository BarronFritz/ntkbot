class NTK_Fishing extends NTK_Task {
    OnInit() {
        ; Write your initilization code here (variables)
    }
    OnTick(dt) {
        ; What happens every tick (logic)
    }
    OnEnd() {
        ; What gets done when this task is destroyed (clean up)
    }
}
